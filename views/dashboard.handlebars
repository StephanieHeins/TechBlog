{{#each posts as |post|}}
    {{> [post-info post]}}

        <div class="d-flex justify-content-center">
            <button type="button" class="btn btn-info">Edit</button>
        </div>
    {{/each}}